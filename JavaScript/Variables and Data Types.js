// Task 1: Variables and Data Types
// Write JavaScript code to declare variables and assign values to them for the following data types:
// Integer
// String
// Boolean
// Array (store a few numbers in it)
// Object (create an object with a few key-value pairs)

// Make sure to use var, let, and const for variable declarations to understand the scoping differences.

const str = 'Hello';
let num = 10;
let flag = true;

const arr = [];
const arr2 = new Array(5).fill(0);

const obj = {
	title: 'Concert',
	price: 399,
	date: 13092023,
};

console.log(str);
console.log(num);
console.log(flag);
console.log(arr);
console.log(arr2);
console.log(obj);
